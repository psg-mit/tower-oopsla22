(include_subdirs unqualified)

(library
 (name lib)
  (modules args ast_ ast_pp ast check errors interp ir_ ir_lower ir_pp ir symbol)
  (libraries core)
  (preprocess (pps ppx_deriving.show ppx_deriving.eq ppx_jane))
)
